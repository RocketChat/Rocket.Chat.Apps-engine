import { AppManager } from '../AppManager';

export class AppLicenseManager {
    constructor(private readonly manager: AppManager) {

    }

    // tslint:disable-next-line: no-empty
    public async validate(license: string) {
    }
}
